// Package providers implements integrations with various LLM API providers.
// It handles the communication with AI services, including message formatting,
// tool registration, and response parsing. Each provider implements the
// models.Provider interface, allowing them to be used interchangeably.
package providers